#!/usr/bin/env node

'use strict';

var path = require('path');
var backup = require(path.resolve(__dirname, '../kibana4-backup'));

backup(function(err){
  if(err)
    process.exit(1);
  process.exit(0);
});